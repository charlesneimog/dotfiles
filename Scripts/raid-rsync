#!/bin/bash

# Define o display
DISPLAY=:0
DBUS_SESSION_BUS_ADDRESS=unix:path=/run/user/1000/bus

# Envia a notificação para o usuário
export DISPLAY
export DBUS_SESSION_BUS_ADDRESS
su "neimog" -c "/usr/bin/notify-send 'RAID Sync' 'RAID Sync started' -t 5000" >> /tmp/raid-rsync.log 2>&1"

